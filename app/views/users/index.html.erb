<h2>投稿一覧</h2>
<% @users.each do |user| %>
  <p>
    <%= user.name %>
    <%= user.age %>
    <%= link_to "削除", user_path(user), method: :delete, data: {confirm: "削除しますか？"} %>
    <%= link_to "編集", edit_user_path(user), method: :get %>
  </p>
<% end %>
<%= link_to "新規投稿", new_users_path %>